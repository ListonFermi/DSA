<h1>Heap Sort</h1>

Heap sort is a sorting algorithm that uses heap data structure.
Using heap data structure, we can heapify the input array and extract each element from the root until we get the sorted array.