Data structures: 
	The form in which we store the datas with its addresses in different formats, so that we can acess the necessary ones with the use of those addressess.

Algorithms:
	Algorithmsa are the way which we write the code to do certain operations. We follow a pattern of code to solve the problems in a efficient way, those are called algorithms.